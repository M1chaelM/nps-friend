\documentclass[addpoints]{exam}

%These tell TeX which packages to use.
\usepackage{array,epsfig}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage{amsxtra}
\usepackage{amsthm}
\usepackage{../mlextra} % must be below ams packages
\usepackage{mathrsfs}
\usepackage{color}
\usepackage{array}
\usepackage{graphicx}
\graphicspath{ {../../art/} }
\usepackage{bm}
\usepackage{tikz}
\usepackage{multicol}

%Pagination stuff.
\setlength{\topmargin}{-.3 in}
\setlength{\oddsidemargin}{0in}
\setlength{\evensidemargin}{0in}
\setlength{\textheight}{9.in}
\setlength{\textwidth}{6.5in}



%\pagestyle{empty} 
%\footer{}{\thepage}{}


\begin{document}


\noindent
\begin{tabular*}{\textwidth}{l @{\extracolsep{\fill}} r @{\extracolsep{6pt}} l}
{\large CS3001: Foundations of Computer Science} &  \makebox[3in]{\large Name:\enspace\hrulefill}\\
{\large April 8, 2019} & \\
{\large Quiz 1} & 
\end{tabular*}\\

\fbox{\fbox{\parbox{6in}{\textbf{Instructions}: Please answer the questions 
  below to the best of your ability. Be sure to show your work where appropriate. 
   This quiz is closed book, closed notes, closed computer. There are \numpoints\ 
   points in total. }}}\\
\begin{questions}

\question[2]
Let $R$ be a binary equivalence relation on a set $S$, and let $x,y$ be
elements of $S$ such that $xRy$. Which of the following statements \emph{must}
be true?

\begin{checkboxes}
\CorrectChoice $[y]_R = [x]_R$
\CorrectChoice $yRx$.
\choice $[x]_R \cap [y]_R = \emptyset$
\choice $S = [x]_R \cup [y]_R$
\end{checkboxes}


\question Let $\Sigma = \{a,b,c\}$ and define $Z$ to be a relation on $\Sigma^*$ such
that $(x,y) \in Z$ if and only if there exists a string $m \in \Sigma^*$
and such that $x = ym$.
\begin{parts}
\part[2] Which of the following pairs are in $Z$:
\begin{checkboxes}
\CorrectChoice $(baabac,baa)$
\choice $(cabacaca,cabacab)$
\CorrectChoice $(aaaaa,\epsilon)$
\CorrectChoice $(\epsilon,\epsilon)$
\end{checkboxes}

\part[3] Prove $Z$ is antisymmetric.
\begin{solution}[30mm]

\end{solution}

\question
Let $\Sigma = \{a,b\}$. Define $S = \{s \in \Sigma^* | 3 \geq |s|\}$ and
$T = \{t | t = xy \text{ for } x,y \in S\}$.

\begin{parts}
\part[2] What is $|S|$?
\begin{solution}[10mm]

\end{solution}


\part[3] Prove $S \subset T$.
\begin{solution}[30mm]

\end{solution}

\end{parts}

%\question
%
%Let $\Sigma = \{a,b,c\}$. Define $Z$ to be a relation on $\Sigma^+$ such
%that $(x,y) \in Z$ if and only if there exist strings $r,s \in \Sigma^*$
%and string $t \in \Sigma$ such that $x = tr$ and $y = ts$.
%
%\begin{parts}
%\part[2] Which of the following pairs are in $Z$:
%\begin{checkboxes}
%\choice $(cac,bac)$
%\CorrectChoice $(baabaa,bac)$
%\choice $(\epsilon,\epsilon)$
%\CorrectChoice $(aaaaa,a)$
%\end{checkboxes}
%
%\part[3] Prove $Z$ is transitive.
%\begin{solution}[30mm]
%
%\end{solution}
%
%%\bonuspart[1] Assume $Z$ is an equivalence relation. How many equivalence
%%classes are there with respect to $Z$ on $\Sigma^+$?
%\end{parts}


%\newpage

\end{questions}
\end{document}


